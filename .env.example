# PostgreSQL — DB única: la de server_en_palabras (usuario ep_analytics).
# read en schema public (ventas/negocio) + own en schema analytics (registro de dashboards).
DATABASE_URL=
PG_SSL=false

# Meta Ads API
META_AD_ACCOUNT_ID=
META_ACCESS_TOKEN=

# GA4 (Google Analytics Data API) — service account con rol Lector en la property
GA_PROJECT_ID=
GA_PROPERTY_ID=
GA_SERVICE_ACCOUNT_EMAIL=
# La private key va entre comillas; dejá los \n tal cual (el código los convierte)
GA_PRIVATE_KEY=

# Google OAuth (console.cloud.google.com → Credentials → OAuth 2.0 Client ID)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback
SESSION_SECRET=generate-a-random-string-here

# Server
PORT=3000
